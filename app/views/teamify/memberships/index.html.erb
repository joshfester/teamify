<p style="color: green"><%= notice %></p>

<h1>Memberships</h1>

<div id="teams_memberships">
  <% @resources.each do |resource| %>
    <%= render resource %>
    <p>
      <%= link_to "Show this membership", membership_path(resource) %>
    </p>
  <% end %>
</div>

<%= link_to "New membership", new_team_membership_path(@team) %>
