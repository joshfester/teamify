<p style="color: green"><%= notice %></p>

<h1>Invitations</h1>

<div id="invitations">
  <% @resources.each do |resource| %>
    <%= render resource %>
    <p>
      <%= link_to "Show this invitation", resource %>
    </p>
  <% end %>
</div>

<%= link_to "New invitation", new_team_invitation_path(@team) %>
